cat > pi-apps/install << 'EOF'
#!/bin/bash
echo "Installing APK-Download.omg..."

APPDIR="$HOME/Applications/APK-Download.omg"
mkdir -p "$APPDIR"
cp -r * "$APPDIR"

chmod +x "$APPDIR/scripts/"*.sh
echo "Installed successfully."
EOF
